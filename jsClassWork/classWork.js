// Operators
// uniary (one operand); x++
// binary (two operands); x + y
// ternary (three operands); x ? y : z  (condition ? exprIfTrue : exprIfFalse
//    this is the conditional operand





// Loops

// for loop
// for(initialization; CSSConditionRule; increment/decrement) {}
for (let i = 0; i < 3; i++) {
    console.log(i);
}


